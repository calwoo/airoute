# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project

`airoute` — AI routing system (part of the `agent-tooling` monorepo). Stack and purpose TBD. **Update this file once the project is scaffolded.**

## Commands

> Fill in once the project is initialized (e.g. `npm install`, `npm run dev`, `npm test`, `npm run lint`).

## Architecture

> Fill in once the project is scaffolded. Describe the high-level structure, key entry points, and data flow.
